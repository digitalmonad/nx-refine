{
  "name": "nx-refine",
  "$schema": "node_modules/nx/schemas/project-schema.json",
  "sourceRoot": ".",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project nx-refine --web",
  "targets": {
    "refine-cli": {
      "executor": "nx:run-commands",
      "options": {
        "command": "refine"
      }
    }
  }
}
